---
interface Props {
  href: string;
  title: string;
  description: string;
  image: string;
  imageAlt: string;
  category: string;
  date: string;
}
const { href, title, description, image, imageAlt, category, date } = Astro.props;
---
<a href={href} class="post-card card">
  <div class="post-thumb">
    <img src={image} alt={imageAlt} width="800" height="300" loading="lazy" />
  </div>
  <div class="post-body">
    <span class="badge">{category}</span>
    <h3>{title}</h3>
    <p>{description}</p>
    <span class="post-meta">{date}</span>
  </div>
</a>

<style>
  .post-card { padding: 0; overflow: hidden; display: flex; flex-direction: column; color: inherit; }
  .post-thumb { aspect-ratio: 800 / 340; overflow: hidden; background: var(--color-gray-50); }
  .post-thumb img { width: 100%; height: 100%; object-fit: cover; }
  .post-body { padding: 1.5rem; display: flex; flex-direction: column; gap: 0.6rem; flex: 1; }
  .post-body h3 { font-size: 1.1rem; margin: 0; }
  .post-body p { font-size: 0.92rem; margin: 0; }
  .post-meta { margin-top: auto; font-family: var(--font-mono); font-size: 0.72rem; color: var(--color-gray-500); padding-top: 0.5rem; }
</style>
